apiVersion: v1
kind: Service
metadata:
  name: spring-cloud-gateway
spec:
  type: NodePort
  selector:
    app: spring-cloud-gateway
  ports:
    - port: 8762
      targetPort: 8762
      nodePort: 30007